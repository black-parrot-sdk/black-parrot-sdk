variables:
  GIT_STRATEGY: fetch
  GIT_SUBMODULE_STRATEGY: none

default:
    image: registry.gitlab.com/dpetrisko/black-parrot-sdk:latest

stages:
    - build

build:
    stage: build
    script:
        - make sdk -j $CI_NUM_CORES


